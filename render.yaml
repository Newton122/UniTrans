services:
  - type: web
    name: unitrans-backend
    env: python
    plan: starter
    region: oregon
    buildCommand: pip install -r backend-api/requirements.txt
    startCommand: cd backend-api && python manage.py migrate && gunicorn unitrans.wsgi:application --bind 0.0.0.0:$PORT
    envVars:
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: "*"
      - key: SECRET_KEY
        value: "django-insecure-change-me-in-production"
      - key: DATABASE_URL
        fromDatabase: unitrans-db
      - key: CORS_ALLOWED_ORIGINS
        value: "https://unitrans.onrender.com,https://frontend-driver.onrender.com,https://frontend-manager.onrender.com,https://frontend-student.onrender.com"

  - type: web
    name: frontend-driver
    env: node
    nodeVersion: 20
    plan: starter
    region: oregon
    buildCommand: cd frontend-driver && npm ci --include=dev && npm run build
    startCommand: cd frontend-driver && npx next start -p $PORT
    envVars:
      - key: NODE_ENV
        value: production
      - key: NEXT_PUBLIC_API_URL
        value: "https://unitrans.onrender.com"

  - type: web
    name: frontend-manager
    env: node
    nodeVersion: 20
    plan: starter
    region: oregon
    buildCommand: cd frontend-manager && npm ci --include=dev && npm run build
    startCommand: cd frontend-manager && npx next start -p $PORT
    envVars:
      - key: NODE_ENV
        value: production
      - key: NEXT_PUBLIC_API_URL
        value: "https://unitrans.onrender.com"

  - type: web
    name: frontend-student
    env: node
    nodeVersion: 20
    plan: starter
    region: oregon
    buildCommand: cd frontend-student && npm ci --include=dev && npm run build
    startCommand: cd frontend-student && npx next start -p $PORT
    envVars:
      - key: NODE_ENV
        value: production
      - key: NEXT_PUBLIC_API_URL
        value: "https://unitrans.onrender.com"

databases:
  - name: unitrans-db
    engine: postgresql
    version: "15"
    plan: starter
